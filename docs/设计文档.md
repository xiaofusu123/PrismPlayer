# PrismPlayer 设计文档

> 基于项目实际代码分析，最后更新：2026-06-01

---

## 1. 设计目标

PrismPlayer 是一款跨平台音视频播放器，支持本地播放和联机同步播放。

**核心目标**：
- 高性能音视频解码与渲染
- 多客户端实时同步播放
- 跨平台（Windows / Android / iOS）
- 模块化架构，便于扩展和二次开发

---

## 2. 总体架构设计

### 2.1 分层架构

采用 **六层分层架构**，自上而下依次为：

```
UI (Flutter) → FFI (Dart↔C++) → Service (API门面) → Business (业务) → Engine (编解码) → Adapter (基础设施)
```

| 要素 | 决策 | 理由 |
|------|------|------|
| 分层数 | 6 层 | 职责清晰分离，每层可独立开发测试 |
| 客户端/服务端分离 | Client + Server | 联机播放需要中心化的房间管理 |
| 动态库 vs 静态库 | Engine/Service 用动态库，Adapter 用静态库 | 动态加载和热插拔；基础设施需要内联优化 |

### 2.2 Client/Server 分离

```
Client (Windows/Android/iOS)          Server (Ubuntu Linux)
     |                                        |
     |  WebSocket / HTTP                      |
     +----------------------------------------+
          房间管理、消息转发、用户认证
```

---

## 3. Engine 层设计

### 3.1 音频引擎

#### 选型：WASAPI 共享模式 (Windows)

| 方案 | 延迟 | CPU 占用 | 独占性 | 选定 |
|------|------|----------|--------|------|
| WASAPI Exclusive | 极低 | 低 | 独占设备 | ❌ |
| **WASAPI Shared** | 低 | 低 | 与系统混音 | ✅ |
| DirectSound | 高 | 低 | 共享 | ❌ |
| WaveOut | 极高 | 低 | 共享 | ❌ |

**选择理由**：WASAPI 共享模式在延迟和兼容性之间取得最佳平衡，用户播放时仍可听到系统通知音。

#### 跨平台扩展

```cpp
// 当前实现
AudioEngineWasapiShared  // Windows

// 计划扩展 (通过工厂模式)
AudioEngineAAudio        // Android
AudioEngineAudioUnit     // iOS/macOS
```

#### 工厂模式设计

```cpp
// 抽象接口
class AudioEngineFactory {
public:
    virtual std::unique_ptr<AudioEngine> create_audio_engine() = 0;
};

// Windows 平台
class AudioEngineWasapiSharedFactory : public AudioEngineFactory {
    std::unique_ptr<AudioEngine> create_audio_engine() override {
        return std::make_unique<AudioEngineWasapiShared>();
    }
};
```

### 3.2 视频引擎

#### 选型：Vulkan 渲染

| 方案 | 性能 | 跨平台 | 学习曲线 | 选定 |
|------|------|--------|----------|------|
| **Vulkan** | 极高 | ✅ Win/Android/Linux | 高 | ✅ |
| DirectX 12 | 极高 | ❌ 仅Windows | 中 | ❌ |
| OpenGL | 中 | ✅ | 低 | ❌ |
| Metal | 极高 | ❌ 仅Apple | 中 | ❌ |

**选择理由**：
- Vulkan 是唯一同时覆盖 Windows、Android、Linux 三大平台的现代 GPU API
- 显式资源管理（VkImage → Texture）保证视频渲染低延迟

#### 解码管线

```
MP4/MKV 文件
    ↓ FileAdapter
FFmpeg Demuxer (解封装)
    ↓
├── Video Stream → FFmpeg Video Decoder → AVFrame
│                                               ↓
│                                     Vulkan Texture Upload
│                                               ↓
│                                     GLFW Window Render
│
└── Audio Stream → FFmpeg Audio Decoder → PCM Buffer
                                                ↓
                                     SoundTouch (变速/变调)
                                                ↓
                                     WASAPI 音频设备输出
```

---

## 4. Business 层设计

### 4.1 AV Sync 同步算法

#### 同步策略

```
AudioClock (主时钟) ← 音频不可中断，作为时间基准
       |
       v
VideoClock ← 对比 AudioClock 计算漂移
       |
       ├── 超前 → 等待 (Sleep)
       ├── 滞后 → 丢帧 (Drop Frame)
       └── 同步 → 渲染当前帧
```

#### 同步状态机

```cpp
enum class sync_state {
    UNINIT,         // 未初始化
    CALIBATING,     // 校准中（初始缓冲）
    SYNCHRONIZED,   // 已同步
    AHEAD,          // 音频超前
    BEHIND,         // 音频滞后
    DISABLE,        // 单播放模式（不启用同步）
    ERROR           // 错误状态
};
```

### 4.2 Network 通信设计

#### 协议栈

| 协议 | 用途 | 库 |
|------|------|-----|
| WebSocket | 实时信令（加入/离开房间、播放控制） | `ixwebsocket` |
| HTTP | REST API（登录、房间列表） | `asio` |
| P2P | 文件直传（计划） | `asio` |

#### 异步模型

选择 **ASIO 异步 I/O** 而非传统同步 + 线程池方案：

```
同步+线程池：                    ASIO异步：
[Thread1] → 等待 → 回调        [单线程EventLoop]
[Thread2] → 等待 → 回调        ├── Socket1 ready
[Thread3] → 等待 → 回调        ├── Socket2 ready
浪费线程资源                    └── Timer event
```

**理由**：C10K 问题下单线程异步模型内存/CPU 效率远超多线程同步模型。

---

## 5. Vendor 库设计

### 5.1 ThreadPool

动态伸缩线程池设计：

```
min_threads (默认2)  ←→  max_threads (默认4)
       |                       |
       v                       v
  [Worker1] [Worker2]  ←→  [Worker3] [Worker4]
       |         |              |         |
       +----+----+      ←→      +----+----+
            |                          |
        TaskQueue (std::queue<function<void*()>>)
```

| 特性 | 说明 |
|------|------|
| 动态伸缩 | 空闲时缩至 min，繁忙时扩至 max |
| 任务队列 | FIFO 队列，支持任意返回值的函数 |
| 线程安全 | `std::mutex` + `std::condition_variable` |

### 5.2 MemoryPool

自定义内存池，减少频繁 `new/delete` 导致的内存碎片。

**应用场景**：
- 频繁的 AVFrame/AVPacket 分配
- 网络数据包缓冲区

---

## 6. 数据库设计

### 6.1 客户端：SQLite

| 表 | 说明 |
|----|------|
| `accounts` | 账号名、昵称、邮箱、密码哈希 |
| `audio_files` | 歌曲名、路径、大小、封面、编码格式、时长、位深、采样率、码率、声道数、声道布局、歌词路径 |
| `video_files` | 文件名、路径、大小、作者、分辨率、色深、时长、编码格式、帧率、码率 |
| `settings` | 用户偏好设置（键值对） |

### 6.2 服务端：MySQL

用于房间状态持久化、用户信息集中管理。

---

## 7. 关键设计模式

| 模式 | 应用位置 | 说明 |
|------|----------|------|
| **工厂模式** | Engine 层 | 平台特定引擎创建 (`AudioEngineFactory`) |
| **门面模式** | Service 层 | 组合 Business 功能，提供统一 API |
| **观察者模式** | Sync → Engine | Engine 回调帧状态给同步模块 |
| **适配器模式** | Adapter 层 | 平台 API 适配（文件/数据库/网络） |
| **状态模式** | AV Sync | 播放/暂停/Seek 状态转换 |

---

## 8. 安全设计

| 模块 | 方案 | 库 |
|------|------|-----|
| 密码存储 | SHA-256 哈希 + Salt | `openssl` |
| WebSocket 加密 | TLS/SSL | `openssl` |
| 文件传输 | 端到端加密（计划） | `openssl` |

---

## 9. 未来扩展

| 方向 | 说明 |
|------|------|
| 移动端 | Android AAudio + iOS AudioUnit 音频引擎 |
| 硬件解码 | NVIDIA NVDEC / Intel QSV / AMF |
| HDR 视频 | Vulkan HDR 渲染管线 + libplacebo |
| 投屏 | DLNA / Chromecast 协议支持 |
| 插件系统 | Lua/Python 脚本扩展 |
