# PrismPlayer 数据流文档

> 基于项目实际架构，最后更新：2026-06-04

---

## 1. 整体数据流向

项目采用分层架构，数据从 UI 层逐层向下传递到 Engine 层处理，结果逐层向上回调。

\\mermaid
sequenceDiagram
    participant UI as UI 层<br/>(Flutter)
    participant FFI as FFI 层<br/>(待建)
    participant SVC as Service 层<br/>(client/service/)
    participant BIZ as Business 层<br/>(av_sync + network)
    participant ENG as Engine 层<br/>(Audio/Video)
    participant ADP as Adapter 层<br/>(File/DB/Net)

    Note over UI: 用户操作
    UI->>FFI: 1. 点击登录
    FFI->>SVC: 2. login(user, pass)
    SVC->>BIZ: 3. 验证账号
    BIZ->>ADP: 4. DBAdapter 查询 SQLite
    ADP-->>BIZ: 5. 返回 Token
    BIZ-->>SVC: 6. 验证通过
    SVC-->>FFI: 7. 登录成功
    FFI-->>UI: 8. 跳转主页

    UI->>FFI: 9. 加入房间 123
    FFI->>SVC: 10. joinRoom(123)
    SVC->>BIZ: 11. 建立 WebSocket
    BIZ->>BIZ: 12. ixwebsocket 连接服务器
    BIZ-->>SVC: 13. 服务器通知: 正在播放 A

    SVC->>BIZ: 14. 启动 AV 同步会话
    BIZ->>ENG: 15. commandOpen(A)
    ENG->>ADP: 16. FileAdapter 读取文件

    Note over ENG: FFmpeg 解封装 + 解码
    ENG->>ENG: 17a. 视频: AVFrame to Vulkan Texture
    ENG->>ENG: 17b. 音频: PCM to WASAPI 设备

    ENG-->>BIZ: 18. AV Sync 校准 (漂移计算)
    BIZ-->>SVC: 19. onFramePresented(TextureID)
    SVC-->>FFI: 20. 通知 UI 更新
    FFI-->>UI: 21. 渲染纹理 Handle
\
---

## 2. 播放控制数据流

\\mermaid
sequenceDiagram
    participant UI as UI 层
    participant FFI as FFI 层
    participant SVC as Service 层
    participant BIZ as Business 层
    participant ENG as Engine 层

    UI->>FFI: 播放 / 暂停 / Seek
    FFI->>SVC: control(cmd)
    SVC->>BIZ: AV Sync 状态更新
    BIZ->>ENG: AudioEngine::pause()
    BIZ->>ENG: VideoEngine::seek(pts, mode)
    ENG-->>BIZ: AudioSyncInfo 返回
    ENG-->>BIZ: VideoSyncInfo 返回
    ENG-->>BIZ: RenderMetadata 返回
    BIZ-->>SVC: 帧就绪
    SVC-->>FFI: 回调
    FFI-->>UI: 刷新画面
\
---

## 3. Server 端数据流

\\mermaid
sequenceDiagram
    participant CLIENT as Client
    participant NET as Network 层<br/>(ASIO + WS)
    participant BIZ as Business 层
    participant ADP as Adapter 层
    participant DB as MySQL

    CLIENT->>NET: WebSocket 连接
    NET->>BIZ: 房间管理
    BIZ->>ADP: 数据库操作
    ADP->>DB: SQL 查询 / 写入
    DB-->>ADP: 结果
    ADP-->>BIZ: 返回
    BIZ-->>NET: 房间状态
    NET-->>CLIENT: 消息广播
\
---

## 4. 层级职责一览

| 层级 | 目录 | 状态 | 职责 |
|------|------|------|------|
| **UI 层** | \client/UI/\ | 仅有占位 | Flutter 界面、用户交互 |
| **FFI 层** | \client/FFI/\ | 占位文件 | Dart to C++ 桥接 |
| **Service 层** | \client/service/\ | C API已实现 | API 门面，组合业务功能 |
| **Business 层** | \client/business/\ | av_sync已实现, network占位 | AV 同步算法、网络通信 |
| **Engine 层** | \client/engine/\ | **最完善** | FFmpeg 解码 + Vulkan/WASAPI 渲染 |
| **Adapter 层** | \client/adapter/\ | 骨架 | 文件/数据库/网络基础设施 |

---

## 5. 核心接口速查

| 接口 | 头文件 | 关键方法 |
|------|--------|----------|
| \AudioEngine\ | \ngine/include/AudioEngine.h\ | \init()\, \play()\, \pause()\, \seek()\, \get_sync_info()\ |
| \VideoEngine\ | \ngine/include/VideoEngine.h\ | \init()\, \play()\, \pause()\, \seek()\, \get_render_result()\ |
| \AudioEngineFactory\ | \ngine/include/AudioEngineFactory.h\ | \create_audio_engine()\ |
| \VideoEngineFactory\ | \ngine/include/VideoEngineFactory.h\ | \create_video_engine()\ |
| \AudioDecoder\ | \practice/AudioDecoder.h\ | \init()\, \sync_decode()\, \sync_decode()\, estart()\, \stop()\, \lush()\ |

---

## 6. 依赖库数据流角色

| 依赖库 | 所在层 | 用途 |
|--------|--------|------|
| \fmpeg\ | Engine | 音视频解封装、解码 |
| \ulkan\ | Engine | 视频纹理渲染 |
| \soundtouch\ | Engine | 音频变速变调 |
| \glfw3\ | Engine | 窗口管理 |
| \sio\ | Business (Network) | 异步网络 I/O |
| \ixwebsocket\ | Business (Network) | WebSocket 客户端 |
| \spdlog\ | 全局 | 日志输出 |
| lohmann-json\ | Adapter | JSON 配置解析 |
